% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docs.R
\name{db_describe_column}
\alias{db_describe_column}
\title{Describe a column}
\usage{
db_describe_column(
  section = NULL,
  dataset = NULL,
  schema = "main",
  table = NULL,
  column,
  description = NULL,
  units = NULL,
  notes = NULL,
  public = NULL
)
}
\arguments{
\item{section}{Section name (hive mode only)}

\item{dataset}{Dataset name (hive mode only)}

\item{schema}{Schema name (DuckLake mode, default "main")}

\item{table}{Table name (DuckLake mode only)}

\item{column}{Column name to document}

\item{description}{Description of what the column contains}

\item{units}{Units of measurement (optional)}

\item{notes}{Additional notes (optional)}

\item{public}{Logical. If TRUE, sync column docs to public catalog (requires
dataset to already be public). If NULL (default), auto-sync if dataset is
already public. (Hive mode only)}
}
\value{
Invisibly returns the column metadata
}
\description{
Add documentation to a specific column in a dataset or table.

In hive mode, you can set \code{public = TRUE} to include the column documentation
in the public catalog. The dataset must already be public (use
\code{db_describe(public = TRUE)} first).
}
\examples{
\dontrun{
db_connect()
db_describe_column(
  section = "Trade",
  dataset = "Imports",
  column = "value",
  description = "Import value in thousands",
  units = "EUR (thousands)",
  public = TRUE
)
}
}
