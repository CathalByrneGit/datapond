% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{db_file_stats}
\alias{db_file_stats}
\title{Get file statistics for DuckLake tables}
\usage{
db_file_stats(schema = NULL, table = NULL)
}
\arguments{
\item{schema}{Schema name (default "main"). Use NULL for all schemas.}

\item{table}{Table name. Use NULL for all tables.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{schema_name}: Schema containing the table
\item \code{table_name}: Table name
\item \code{file_count}: Number of Parquet files
\item \code{total_rows}: Total row count across all files
\item \code{total_bytes}: Total size in bytes
\item \code{avg_file_bytes}: Average file size
\item \code{avg_rows_per_file}: Average rows per file
}
}
\description{
Returns information about the Parquet files backing each table,
including file counts, sizes, and row counts. Use this to identify tables
that would benefit from compaction.

\strong{Indicators that compaction may help:}
\itemize{
\item High file count with small average file size (< 10 MB)
\item Many more files than expected for the data volume
\item Slow query performance on tables with many files
}
}
\examples{
\dontrun{
db_connect()

# Check all tables
db_file_stats()

# Check a specific table
db_file_stats(table = "imports")

# Find tables needing compaction (many small files)
stats <- db_file_stats()
stats[stats$file_count > 100 & stats$avg_file_bytes < 1e7, ]
}
}
\seealso{
\code{\link[=db_compact]{db_compact()}} to merge small files
}
