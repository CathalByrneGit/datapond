% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_connect.R
\name{db_lake_connect_section}
\alias{db_lake_connect_section}
\title{Connect to a DuckLake section via master catalog}
\usage{
db_lake_connect_section(
  section,
  master_path = NULL,
  duckdb_db = ":memory:",
  catalog = NULL,
  catalog_type = NULL,
  threads = NULL,
  memory_limit = NULL
)
}
\arguments{
\item{section}{Section name (must be registered in master catalog)}

\item{master_path}{Path to master catalog (uses default if not specified)}

\item{duckdb_db}{DuckDB database file path. Use ":memory:" for in-memory.}

\item{catalog}{DuckLake catalog name inside DuckDB}

\item{catalog_type}{Type of catalog backend (auto-detected from path if not specified)}

\item{threads}{Number of DuckDB threads (NULL leaves default)}

\item{memory_limit}{e.g. "4GB" (NULL leaves default)}
}
\value{
DuckDB connection object
}
\description{
Connects to a registered section by looking up its catalog
and data paths from the master discovery catalog. This enables the
\code{public} parameter on \code{db_describe()} to sync to the master catalog.
}
\examples{
\dontrun{
# Connect to a section registered in master catalog
db_lake_connect_section("trade")

# Now db_describe(public=TRUE) will sync to master catalog
db_describe(table = "imports", description = "...", public = TRUE)
}
}
