% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{db_rollback}
\alias{db_rollback}
\title{Rollback a table to a previous snapshot}
\usage{
db_rollback(
  schema = "main",
  table,
  version = NULL,
  timestamp = NULL,
  commit_author = NULL,
  commit_message = NULL
)
}
\arguments{
\item{schema}{Schema name (default "main")}

\item{table}{Table name}

\item{version}{Snapshot version to rollback to (integer)}

\item{timestamp}{Timestamp to rollback to (POSIXct or character string)}

\item{commit_author}{Optional author for the rollback commit}

\item{commit_message}{Optional message for the rollback commit (defaults to auto-generated)}
}
\value{
Invisibly returns the qualified table name
}
\description{
Restores a table to its state at a specific snapshot version or timestamp.
This creates a new snapshot with the rolled-back data.
}
\examples{
\dontrun{
db_connect()

# Rollback to a specific version
db_rollback(table = "products", version = 5)

# Rollback to a specific time
db_rollback(table = "products", timestamp = "2025-01-15 00:00:00")
}
}
