% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{db_compact}
\alias{db_compact}
\title{Compact small files in a DuckLake table}
\usage{
db_compact(schema = "main", table = NULL, max_files = NULL)
}
\arguments{
\item{schema}{Schema name (default "main"). Use NULL to compact all schemas.}

\item{table}{Table name. Use NULL to compact all tables in the schema.}

\item{max_files}{Maximum number of files to compact in one operation.
Lower values use less memory. Default NULL compacts all eligible files.}
}
\value{
Invisibly returns a list with compaction results
}
\description{
Merges small Parquet files into larger ones to improve query
performance. When data is written in small batches, DuckLake creates many
small files which slows down reads. Compaction consolidates these files.

\strong{When to compact:}
\itemize{
\item After many small inserts (e.g., streaming data, row-by-row imports)
\item When \code{db_file_stats()} shows high file counts with small average sizes
\item Before running large analytical queries on frequently-updated tables
}

\strong{Important notes:}
\itemize{
\item Compaction is memory-intensive; use \code{max_files} to limit batch size
\item Files with different schema versions cannot be merged together
\item Old files are not immediately deleted; run \code{db_cleanup_files()} after
}
}
\examples{
\dontrun{
db_connect()

# Check if compaction is needed
db_file_stats()

# Compact a specific table
db_compact(table = "imports")

# Compact with memory limit (process 500 files at a time)
db_compact(table = "imports", max_files = 500)

# Compact all tables in a schema
db_compact(schema = "trade")

# Compact entire catalog
db_compact()

# Clean up old files after compaction
db_cleanup_files()
}
}
\seealso{
\code{\link[=db_file_stats]{db_file_stats()}} to check file statistics before compacting,
\code{\link[=db_cleanup_files]{db_cleanup_files()}} to remove old files after compaction
}
