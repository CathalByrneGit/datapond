% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{db_cleanup_files}
\alias{db_cleanup_files}
\title{Clean up orphaned files from DuckLake storage}
\usage{
db_cleanup_files(dry_run = TRUE)
}
\arguments{
\item{dry_run}{If TRUE (default), shows what would be deleted without deleting.
Set to FALSE to actually remove files.}
}
\value{
Invisibly returns the count of files cleaned up (or that would be)
}
\description{
Removes data files that are no longer referenced by any snapshot.
Run this after \code{db_vacuum()} or \code{db_compact()} to reclaim disk space.

\strong{When files become orphaned:}
\itemize{
\item After \code{db_vacuum()} removes old snapshots
\item After \code{db_compact()} merges files (old small files become orphaned)
\item After failed transactions that wrote partial data
}
}
\examples{
\dontrun{
db_connect()

# Compact files then clean up
db_compact(table = "imports")
db_cleanup_files(dry_run = FALSE)

# Vacuum old snapshots then clean up
db_vacuum(older_than = "30 days", dry_run = FALSE)
db_cleanup_files(dry_run = FALSE)
}
}
\seealso{
\code{\link[=db_vacuum]{db_vacuum()}} to remove old snapshots, \code{\link[=db_compact]{db_compact()}} to merge files
}
