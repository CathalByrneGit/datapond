% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{db_enable_logging}
\alias{db_enable_logging}
\title{Enable DuckDB/DuckLake logging}
\usage{
db_enable_logging(enable = TRUE, log_type = c("query", "metadata", "all"))
}
\arguments{
\item{enable}{If TRUE, enable logging. If FALSE, disable.}

\item{log_type}{Type of logging: "query" for SQL queries, "metadata" for
DuckLake metadata operations, or "all" for both. Default "query".}
}
\value{
Invisibly returns TRUE on success
}
\description{
Enables logging for debugging and monitoring DuckLake operations.
DuckLake registers a dedicated log type for metadata queries. The built-in
QueryLog type can trace all SQL queries including internal ones.
}
\examples{
\dontrun{
db_connect(...)

# Enable query logging
db_enable_logging(TRUE)

# Run some operations...
db_read(table = "users") |> head()

# View logs
db_query("SELECT * FROM duckdb_logs() ORDER BY timestamp DESC LIMIT 20")

# Disable logging
db_enable_logging(FALSE)
}
}
