% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{db_comment}
\alias{db_comment}
\title{Add comment/metadata to table or column}
\usage{
db_comment(schema = "main", table, column = NULL, comment)
}
\arguments{
\item{schema}{Schema name (default "main")}

\item{table}{Table name}

\item{column}{Optional column name. If NULL, comment is added to the table.}

\item{comment}{The comment - either a string or a list. Lists are converted
to JSON. Use NULL to remove comment.

For tables, common list fields: \code{description}, \code{owner}, \code{tags}, \code{lineage_sources},
\code{lineage_transformation}.

For columns, common list fields: \code{description}, \code{units}, \code{notes}.}
}
\value{
Invisibly returns TRUE on success
}
\description{
Adds metadata to a table or column using DuckLake's native
COMMENT ON statement. Comments are stored in the DuckLake catalog and
support time travel.

The comment can be a simple string or a list with structured metadata.
Lists are automatically converted to JSON for storage.
}
\examples{
\dontrun{
db_connect(...)

# Simple string comment
db_comment(table = "users", comment = "Active user accounts")

# Structured table metadata (stored as JSON)
db_comment(table = "imports", comment = list(
  description = "Monthly import values by country",
  owner = "Trade Section",
  tags = c("trade", "monthly", "official")
))

# Structured column metadata
db_comment(table = "imports", column = "value", comment = list(
  description = "Import value",
  units = "EUR (thousands)"
))

# Remove comment
db_comment(table = "users", comment = NULL)
}
}
\seealso{
\code{\link[=db_get_docs]{db_get_docs()}} to retrieve documentation
}
