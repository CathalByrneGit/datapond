% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced.R
\name{db_export_iceberg}
\alias{db_export_iceberg}
\title{Export a DuckLake table as Iceberg format (EXPERIMENTAL)}
\usage{
db_export_iceberg(
  schema = "main",
  table,
  path = NULL,
  catalog_type = c("hadoop", "hive", "rest")
)
}
\arguments{
\item{schema}{Schema name (default "main")}

\item{table}{Table name}

\item{path}{Output path for Iceberg metadata. If NULL, uses the table's
existing data path with an \verb{iceberg/} subdirectory.}

\item{catalog_type}{Type of Iceberg catalog to generate: "hadoop" (default),
"hive", or "rest".}
}
\value{
Invisibly returns the output path
}
\description{
Exports a DuckLake table to Iceberg format for compatibility
with other data lakehouse engines (Spark, Trino, Presto, etc.).

\strong{Note:} This is experimental. DuckLake 0.3+ supports Iceberg interoperability
via \verb{COPY FROM DATABASE ducklake TO iceberg_catalog}. This function attempts
to use internal DuckLake Iceberg functions which may not be available.
}
\examples{
\dontrun{
db_connect()

# Export to Iceberg format
db_export_iceberg(table = "sales")

# Export to specific location
db_export_iceberg(table = "sales", path = "/data/iceberg/sales")

# Export for Hive Metastore compatibility
db_export_iceberg(table = "sales", catalog_type = "hive")
}
}
\seealso{
\code{\link[=db_iceberg_metadata]{db_iceberg_metadata()}} to view Iceberg metadata
}
