% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_write.R
\name{db_lake_write}
\alias{db_lake_write}
\title{Write a DuckLake table (overwrite/append)}
\usage{
db_lake_write(
  data,
  schema = "main",
  table,
  mode = c("overwrite", "append"),
  partition_by = NULL,
  commit_author = NULL,
  commit_message = NULL
)
}
\arguments{
\item{data}{data.frame/tibble}

\item{schema}{Schema name (default "main")}

\item{table}{Table name}

\item{mode}{"overwrite" or "append"}

\item{partition_by}{Optional character vector of column names to partition by.
Only valid for mode = "overwrite". On overwrite, if not specified, existing
partitioning is preserved.}

\item{commit_author}{Optional author for DuckLake commit metadata}

\item{commit_message}{Optional message for DuckLake commit metadata}
}
\value{
Invisibly returns the qualified table name
}
\description{
Write a DuckLake table (overwrite/append)
}
\examples{
\dontrun{
# Basic overwrite
db_lake_write(my_data, table = "imports")

# With schema
db_lake_write(my_data, schema = "trade", table = "imports")

# With partitioning (overwrite mode only)
db_lake_write(my_data, schema = "trade", table = "imports",
              partition_by = c("year", "month"))

# Append mode with commit info
db_lake_write(my_data, table = "imports", mode = "append",
              commit_author = "jsmith",
              commit_message = "Added Q3 data")
}
}
