% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{db_set_partitioning}
\alias{db_set_partitioning}
\title{Set partitioning for a DuckLake table}
\usage{
db_set_partitioning(schema = "main", table, partition_by)
}
\arguments{
\item{schema}{Schema name}

\item{table}{Table name}

\item{partition_by}{Character vector of column names or expressions to partition by.
Use NULL to remove partitioning.}
}
\value{
Invisibly returns TRUE on success
}
\description{
Configures partition keys for a DuckLake table. When partitioning
is set, new data written to the table will be split into separate files
based on the partition key values.

Partitioning enables:
\itemize{
\item Efficient query pruning (only read relevant partitions)
\item Potential folder-based access control at partition level
\item Better data organization for time-series data
}

Note: Existing data is not reorganized - only new inserts are partitioned.
}
\examples{
\dontrun{
db_lake_connect(...)

# Partition by year and month columns
db_set_partitioning("trade", "imports", c("year", "month"))

# Partition using date functions
db_set_partitioning("trade", "imports", c("year(date)", "month(date)"))

# Remove partitioning (new data won't be partitioned)
db_set_partitioning("trade", "imports", NULL)
}
}
