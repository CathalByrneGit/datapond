% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced.R
\name{db_flush_inlined}
\alias{db_flush_inlined}
\title{Flush inlined data to parquet files}
\usage{
db_flush_inlined(schema = "main", table = NULL)
}
\arguments{
\item{schema}{Schema name (default "main"). Use NULL for all schemas.}

\item{table}{Table name. Use NULL for all tables in the schema.}
}
\value{
Invisibly returns a data.frame with schema_name, table_name, rows_flushed
}
\description{
Writes inlined data (small inserts/deletes stored in the catalog)
to parquet files. DuckLake automatically inlines writes with fewer rows than
\code{data_inlining_row_limit} (default 10). Use this function to consolidate
inlined data into proper parquet files.
}
\examples{
\dontrun{
db_connect()

# Small writes are automatically inlined
db_write(small_batch, table = "events", mode = "append")  # < 10 rows

# Flush inlined data for a specific table
db_flush_inlined(table = "events")

# Flush all inlined data in a schema
db_flush_inlined(schema = "raw", table = NULL)

# Flush all inlined data in catalog
db_flush_inlined(schema = NULL, table = NULL)
}
}
\seealso{
\code{\link[=db_set_inline_threshold]{db_set_inline_threshold()}} to configure inlining threshold
}
